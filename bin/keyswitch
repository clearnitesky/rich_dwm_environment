#!/bin/sh

which=$(echo "" | dmenu -p "Select Keyboard Layout: ")
# for debugging: echo "selected $which"

if [ $which = "us" ]; then
	echo "🇺🇸 American" > ~/.status/keym
elif [ $which = "gb" ]; then
	echo "🇬🇧 British" > ~/.status/keymap
elif [ $which = "es" ]; then
	echo "🇪🇸 Español" > ~/.status/keymap
else notify-send " Keyboard Switch Cancelled " && exit 1
fi

notify-send " Keyboard Layout Changed to $(cat ~/.status/keymap) "
setxkbmap $which
rsbar
