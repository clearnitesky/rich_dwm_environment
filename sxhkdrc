### PROGRAM LAUNCH ###

### Config Progs ###

alt + shift + w
	st -e nmtui
alt + shift + t
	st -e vtop
alt + shift + e
	dmenuunicode
alt + shift + r
	arandr
alt + shift + a
	st -e pulsemixer
alt + shift + s
	st -e sudo wavemon
alt + shift + c
	clipmenu -sf \#161616 -sb \#eeeeee
alt + shift + m
	notify-send "Checking for mail." -t 1500; mbsync -a
alt + shift + F4
	scrot
alt + shift + p
	st
alt + shift + space
	dmenu_run

### Standard Progs ###

alt + w
	brave
alt + r
	st -e ranger
alt + t
	st -e tremc
alt + m
	st -e neomutt
alt + f
	pcmanfm
alt + g
	gointernet
alt + l
	frescobaldi
alt + n
	liferea
alt + c
	notify-send "$(py_crypto_check)"
alt + F5
	record
alt + F6
	killall ffmpeg; notify-send "Recording ended"


### STATUS ###
alt + s
	 notify-send "Time: ⏰ $(date +'%H:%M')"; notify-send "Date:  📅 $(date +'%B %d, %Y')"; notify-send "Power:  $(powercheck)"; notify-send "Sound:  $(soundcheck)"; notify-send "Net:  $(netstatus)"


### HOT KEYS ETC ###

#XF86Launch1
#	xset dpms force off
XF86TouchpadToggle
 	(synclient | grep "TouchpadOff.*1" && synclient TouchpadOff=0) || synclient TouchpadOff=1
XF86Touchpad{Off,On}
	synclient TouchpadOff={1,0}

XF86MonBrightnessDown
	brightnessctl s 5%-
XF86MonBrightnessUp
	brightnessctl s +5%

# AUDIO KEYS

XF86AudioMute
	amixer sset Master toggle; notify-send "$(~/bin/soundcheck)" -t 500; rsbar
XF86Audio{Raise,Lower}Volume
	amixer sset Master 5%{+,-}; notify-send "$(~/bin/soundcheck)" -t 500; rsbar

#XF86Audio{Next,Prev}
#	mpc {next,prev}
#XF86Audio{Pause,Play,Stop}
#	mpc {pause,play,stop}
#XF86Audio{Rewind,Forward}
#	mpc seek {-,+}10
#XF86AudioRecord
#	dmenurecord
#XF86AudioMedia
#	$TERMINAL -e ncmpcpp

#XF86PowerOff
#	prompt "Shutdown computer?" "sudo -A shutdown -h now"
#XF86Sleep
#        notify-send "Sleeping..."; sleep 20; sudo pm-suspend
#	prompt 'Hibernate computer?' 'sudo -A zzz'
#XF86WWW
#	$BROWSER
#XF86DOS
#	$TERMINAL
#XF86TaskPane
#	$TERMINAL -e htop
#XF86Mail
#	$TERMINAL -e thunderbird
#XF86MyComputer
#	$TERMINAL -e $FILE /

# Change display
#super + F3
#	displayselect
# Hibernate
#super + F4
#	prompt 'Hibernate computer?' 'sudo -A zzz -Z'
#super + F5 is unbound.
# Start torrent daemon/open interface
#super + F6
#	torwrap
# toggle torrent daemon
#super + F7
#	td-toggle
# sync email
#super + F8
#	mailsync
# Mount a USB drive or Android device
#super + {F9,F10}
#	{dmenumount,dmenuumount}
